// Vendor
import React from 'react';
import Helmet from 'react-helmet';
// Components
import Layout from '../../components/Layout';

const ${TM:COMPONENT_NAME} = () => (
  <Layout>
    <Helmet title="${TM:COMPONENT_NAME} page" />
    ${TM:COMPONENT_NAME}
  </Layout>
);

export default ${TM:COMPONENT_NAME};

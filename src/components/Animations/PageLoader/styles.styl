.PageLoader {
  position fixed
  top 0
  left 0
  z-index 1000
  width 100%
  height 3px
  overflow hidden
  will-change opacity

  &__line {
    fullsize()
    background $brandColor
    opacity 0
    will-change transform
  }
}
